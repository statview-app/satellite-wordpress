{
    "name": "statview-satellite-wordpress",
    "private": true,
    "description": "Dev tooling for the Statview Satellite WordPress plugin",
    "scripts": {
        "env:start": "wp-env start",
        "env:stop": "wp-env stop",
        "test:integration": "wp-env run tests-cli --env-cwd=wp-content/plugins/satellite-wordpress vendor/bin/pest --configuration=phpunit.integration.xml.dist"
    },
    "devDependencies": {
        "@wordpress/env": "^10.0.0"
    }
}
